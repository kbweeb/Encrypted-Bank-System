# Encrypted-Bank-System
An encrypted bank system in C++ securely manages user accounts, transactions, and balances by storing data in an XOR-encrypted file and protecting access with PIN-based authentication.
